seed: 42
hidden_layer_sizes: [64, 64, 32]
learning_rate: 0.001
epochs: 200
batch_size: 32
input_features: [S0, K, T, r, sigma]
target: binomial_price
train_test_split: 0.8
normalization: zscore
checkpoint_path: models/nn_pricer.pt
